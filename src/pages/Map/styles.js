import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    map: {
        height: '100%',
        width: '100%'
    },
    box: {
        flex: 1
    }
});

export default Styles;